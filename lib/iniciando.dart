import 'package:flutter/material.dart';

class Iniciando extends StatelessWidget {
  const Iniciando({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Practicando'),
    );
  }
}